-- Script test checkpoint
-- Dán vào Delta, chạy, nó sẽ in ra tất cả checkpoint tìm thấy

for _, obj in ipairs(workspace:GetDescendants()) do
    if string.find(obj.Name:lower(), "checkpoint") then
        print("Found:", obj:GetFullName())
    end
end
